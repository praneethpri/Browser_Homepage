import { TitleLimiterPipe } from './title-limiter.pipe';

describe('TitleLimiterPipe', () => {
  it('create an instance', () => {
    const pipe = new TitleLimiterPipe();
    expect(pipe).toBeTruthy();
  });
});
